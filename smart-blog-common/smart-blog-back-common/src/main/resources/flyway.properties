flyway.locations=${db.local.migratget}
flyway.password=${db.password}
flyway.placeholders.commonColums=created_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, created_by CHAR(36) NOT NULL DEFAULT 'system', update_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_by CHAR(36) NOT NULL DEFAULT 'system', version bigint NOT NULL DEFAULT 0
flyway.schemas=${db.schema}
flyway.url=${db.jdbc.url}
flyway.user=${db.username}